plugins {
    id 'java-gradle-plugin'
}

gradlePlugin {
    plugins {
        compileFlags {
            id = 'com.example.compile-flags-per-source-file'
            implementationClass = 'CompileFlagsPerSourceFilePlugin'
        }
    }
}